! Safety Rating
0 = safe
1 = suggestive
2 = mature
3 = explicit
! Author types
0 = artist
1 = character owner


# Notes
### Why `*[data-lucide]` instead of `.lucide`
Stops pop-in when lucide hasn't loaded yet, since the lucide class is only added after the icon has loaded.
If we do this instead, then while the icon isnt loaded there'll just be a blank spot later filled, so no page reflowing